Be consistent with using Path vs PathBuf, use the one that makes sense everywhere

Need to get rid of unused code. If some logic was rewritten elsewhere, get rid of the unused version of it.

Want to index and query on .md files as well, chunked by paragraph splitting